🎯 **What:** Replaced empty `except FileNotFoundError: pass` handlers in `htxbot/strategy_filters.py` and `htxbot/strategy_risk.py` with appropriate `DEBUG`-level log events using `self._log_event`.
💡 **Why:** Empty exception handlers (often called "swallowed exceptions") reduce the system's observability. By logging these cases at the debug level, we preserve the original safe-failure logic (where a missing CSV file isn't an error because it just hasn't been generated yet) while making it possible to trace this path during active debugging or issue diagnosis.
✅ **Verification:** Verified that `self._log_event` is the standard bot logging mechanism. Test execution was performed, revealing a baseline `TypeError` present on the `main` branch, but validating that the new logic did not introduce any syntax errors or new breakages. Code review confirmed the safety and correctness of the change.
✨ **Result:** Enhanced maintainability and debugging capability without altering the production behavior.

*Note: The test suite currently fails locally due to an unrelated baseline issue in `htxbot/signal_engine.py` (`_calculate_signal_indicators` returns None and throws a TypeError when unpacked), which is outside the scope of this code health fix.*
